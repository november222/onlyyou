export type Theme = {
  background: string;
  text: string;
  primary: string;
  secondary: string;
  card: string;
  border: string;
  onBackground?: string;
  onCard?: string;
  onPrimary?: string;
  onSecondary?: string;
  mutedText?: string;
  success?: string;
  danger?: string;
  overlay?: string; // modal/overlay scrims
};

